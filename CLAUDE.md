# CLAUDE.md

## Rules

- **Python env**: Always use `.venv/bin/python` (not system `python3`).
- **Commits**: No `Co-Authored-By` lines. Single-line messages (no body).
- **Dependencies**: Managed in `requirements/*.txt` (used by local dev and Docker).
- **Docs sync**: When modifying code, update CLAUDE.md and ARCHITECTURE.md to reflect changes.
- **No memory**: Never use the auto-memory system (no MEMORY.md, no memory files). All persistent context belongs in CLAUDE.md or ARCHITECTURE.md.
- **Error handling**: App should crash on unexpected errors. `try/except` only for expected, recoverable errors. Custom exceptions in `exceptions.py`.
- **No API backward compat**: Project has no external users yet — don't preserve old Python APIs, function signatures, or import paths. Rename, delete, and rewrite freely; no shims or re-export modules. DB schema changes still go through Django migrations as normal — existing installs must upgrade cleanly.

## Project Overview

OpenOutreach — self-hosted LinkedIn automation for B2B lead generation. Playwright + stealth for browser automation, LinkedIn Voyager API for profile data, Django + Django Admin for CRM (models owned by this project).

## Commands

```bash
# Docker
make build / make up / make stop / make logs / make up-view

# Local dev
make setup    # install deps + browsers + migrate + bootstrap CRM
make run      # run daemon
make admin    # Django Admin at localhost:8000/admin/

# Testing
make test / make docker-test
pytest tests/api/test_voyager.py   # single file
pytest -k test_name                # single test
```

## Architecture (quick reference)

For detailed module docs, see `ARCHITECTURE.md`.

- **Entry**: `manage.py` — stock Django management. `rundaemon` command (migrate → onboard → validate → task queue loop). `manage.py` with no args defaults to `rundaemon`. Onboarding logic in `onboarding.py`: `OnboardConfig` (pure dataclass), `missing_keys()`, `collect_from_wizard()`, single `apply()` write path. Docker `start` script handles Xvfb/VNC, then `exec python manage.py rundaemon`.
- **State machine**: `enums.py:ProfileState` — QUALIFIED → READY_TO_CONNECT → PENDING → CONNECTED → COMPLETED / FAILED. `Deal.connected_at` (crm 0014) stamps the first PENDING→CONNECTED transition (set in `linkedin/db/deals.py:set_profile_state`) — feeds the dashboard "invitations acceptées/jour" chart. Deal.state is a CharField with ProfileState choices (no Stage model). `Outcome` (converted/not_interested/wrong_fit/no_budget/has_solution/bad_timing/unresponsive/unknown) on Deal.outcome. `Lead.disqualified=True` = permanent exclusion. LLM rejections = FAILED Deals with wrong_fit outcome (campaign-scoped).
- **Task queue**: `Task` model (persistent). Three types: `connect`, `check_pending`, `follow_up`. Handlers in `linkedin/tasks/`, signature: `handle_*(task, session, qualifiers)`. Task creation is centralized in `linkedin/tasks/scheduler.py` — no other module inserts Task rows. `set_profile_state()` fires `on_deal_state_entered(deal)`, which enqueues the task implied by the new state (CONNECTED → follow_up, PENDING → check_pending). **Connect quota (15/07)**: `claim_next()` serves a DUE connect FIRST while fewer than `EKOALU_DAILY_CONNECT_QUOTA` (default 12) connect tasks have completed today — without this floor, the LOT C type priority starved connects entirely (0 served 08-13/07; qualification runs INSIDE handle_connect, so 0 deals and 0 invitations followed). The quota never applies to a restricted claim (`task_types=`, the read-pacing follow_up pass-through). **Type priority (LOT C 07/07)**: past the quota, `Task.objects.claim_next()` serves, among DUE tasks, follow_up first, then check_pending, then connect (FIFO on `scheduled_at` within a type; `task_types=` restricts the claim) — a permanent connect backlog no longer starves the follow-ups. **Pending-validation recheck backoff (15/07)**: when a follow_up finds (or creates) a PendingOutbound awaiting Richard's validation, the recheck task is re-enqueued in 8-12h jittered (was 4h — ~23 waiting relances cycling at 4h monopolized the daemon's action slots); the actual send is driven by the approved queue, not by this recheck. **Reconcile (LOT C)**: `reconcile(session)` runs at daemon startup, then at most every `RECONCILE_INTERVAL_SECONDS` (30 min) even when the queue never drains, plus the historic "no ready task" trigger. It recovers stale RUNNING rows, seeds one connect per campaign, and re-creates tasks for any active Deal without a pending task (PENDING → check_pending, CONNECTED → follow_up). This is the retry mechanism — a crashed handler leaves a FAILED task with no successor, and the next reconcile re-creates it (cap: `MAX_FAILED_RETRIES=2` per payload per 24h). On 401 (`AuthenticationError`), the daemon marks the task FAILED (no auto re-login); reconcile picks it up.
- **Interception vs échec (LOT C 07/07)**: in `require_approval` mode, the patched `send_connection_request`/`send_raw_message` return the falsy sentinel `linkedin.enums.INTERCEPTED` when the invitation/message is captured as a PendingOutbound (creation AND ABM dedup). Handlers test identity: `handle_follow_up` keeps the Deal **CONNECTED** and re-checks in 8-12h (15/07 backoff; was 4h) (before: the `False` return was read as a send failure → CONNECTED demoted to QUALIFIED, every generated relance broke its own chain); `handle_connect` counts **no** connect attempt (before: 3 waiting-for-Richard cycles = wrongful "Unreachable" disqualification). A real send failure (plain `False`) keeps the old failure behavior.
- **ML pipeline**: GPR (sklearn) + BALD active learning + LLM qualification. Per-campaign models stored in `Campaign.model_blob` (DB). **EKOALU lead routing** (`ekoalu/lead_routing/`, patches `create_enriched_lead` + `get_leads_for_qualification` via `apps.py`): each Lead is attributed to the campaign that discovered it (`LeadDiscovery` model), and a campaign only qualifies *its own* discovered leads instead of the whole base — kills the cross-campaign over-qualification (~97% wrong_fit by construction). Kill-switch `EKOALU_SCOPED_QUALIFICATION=0` or sentinel `data/scoped_qualification_disabled.flag`. Backfill in-flight leads with `manage.py backfill_lead_sources`. Per-campaign targeting criteria (geo + role rules) live in `Campaign.campaign_objective`, applied from the ops Excel via `manage.py apply_campaign_criteria`. **Google sourcing** (`ekoalu/google_sourcing/`, command `manage.py source_via_google`): finds LinkedIn profile URLs via Google search results (`site:linkedin.com/in "Company" "role"`) instead of LinkedIn's own search, creates URL-only leads + `LeadDiscovery` (the daemon enriches/qualifies them later in its own browser session — no second browser). Backend: **Serper.dev** (Google Custom Search JSON API is closed to new customers since 2026). Config: env `SERPER_API_KEY` (1 credit/query, 2,500 free trial credits). ABM-first. **Source routing (12/06)**: ABM campaigns are sourced via Serper ONLY — `ekoalu/google_sourcing/routing.py:native_search_allowed` makes `run_search` skip LinkedIn native keyword search for them (short company names match worldwide homonyms and burn the read cap; kill-switch `EKOALU_ABM_NATIVE_SEARCH=1`). Global persona campaigns (métallier, maçon…) keep native search. **SECTOR campaigns (01/07)**: a third campaign type sourced via Serper by *sector + role* instead of by company — for personas that map to an activity sector, not a named firm (first case: `EKOALU - SECTEUR - Bailleurs sociaux RA`, for the porte-grand-passage / halls angle). `queries.py:SECTOR_SPECS` is a registry keyed by the slug parsed from `EKOALU - SECTEUR - <slug>` (sector anchor + role terms + geo-bias regions); `build_sector_queries` emits `site:linkedin.com/in "<anchor>" "<role>" <region>` (validated live: anchor `"logement social"` beats `"bailleur social"`, appending `Lyon` biases toward Rhône-Alpes). `queries.build_queries` dispatches ABM vs SECTOR; `routing.is_sector_campaign` makes SECTOR Serper-only too (native search off). Both `source_via_google` and `source_via_google_rotate` select `" ABM - " OR " SECTEUR - "`. Keep `roles × regions ≤ --per-campaign-queries` (9). **Daily rotation**: `manage.py source_via_google_rotate` (scheduled task `EKOALU-Serper-Rotation`, 7:00 Mon-Fri via `scripts/serper_rotation.ps1`) serves ABM + SECTOR campaigns least-recently-served first, stops at `--new-leads-target` (30) or `--max-queries` (60 credits). **Dedup against the base (07/07)**: only profiles UNKNOWN to the base (no existing Lead) count toward the per-campaign quota `max_profiles` — Google reserves the same top-10 every day, so known profiles are re-attached to the campaign (LeadDiscovery) at zero quota cost and the remaining role-queries keep running; when a full page is majority-known, the next Google page is fetched (1 credit/page, cap `EKOALU_SERPER_MAX_PAGES`, default 3 — pagination lives in `service.py`, `client.search_linkedin_results` fetches exactly ONE page per call). **Exhaustion**: a campaign with 2 consecutive COMPLETE runs (all role-queries executed, `SourcingResult.all_queries_run`) yielding zero NEW profiles is flagged exhausted (`GoogleSourcingState`, ekoalu migration 0020) and leaves the rotation; a partial run (query budget spent before the end) never counts. `--reset-exhausted` re-arms, and `serper_rotation.ps1` runs it automatically on the first working day of each month (day ≤ 3, Mon-Fri). Shared per-campaign logic in `ekoalu/google_sourcing/service.py`. **URL-only embed priority (07/07)**: `linkedin/pipeline/qualify.py:fetch_qualification_candidates` embeds up to N URL-only leads of the campaign per cycle (env `EKOALU_URLONLY_EMBED_PER_CYCLE`, default 2) BEFORE serving the embedded pool (before, a URL-only lead was only embedded when the pool was empty, which never happened). **Apify-first (07/07)**: when Apify is ready (token + kill-switch + daily cap), each of these embeds is a cookieless Apify fetch — zero Voyager read, read_guard cap untouched; Voyager fallback (one account read, LOT C pacing) on Apify failure or when unconfigured. **Sidebar harvest OFF (07/07)**: `visit_profile` no longer harvests the "people also viewed" profiles visible on a profile page (was up to 10 Voyager reads per visit ≈ 70% of the daily read cap, junk leads attributed to the current campaign with empty query) — kill-switch `EKOALU_SIDEBAR_HARVEST=1` to re-enable; `search_people` (native search RESULTS pages, deliberate persona sourcing) still harvests. **Serper pre-filter (15/06)**: `ekoalu/google_sourcing/prefilter.py:passes_prefilter` écarte un résultat AVANT la lecture LinkedIn si son title/snippet révèle un métier franchement hors-domaine bâtiment (avocat, photographe, postdoc, formateur, data engineer, géomètre…) — économise la lecture des homonymes/hors-cible évidents que Serper ramène malgré la requête `"Company" "role"`. Conservateur (n'exclut QUE l'évident, le tri fin reste au LLM) ; `client.search_linkedin_results` garde title/snippet, `SourcingResult.prefiltered` compte les écartés. Kill-switch `EKOALU_SERPER_PREFILTER=0`. **Pause campagne (15/06)**: `Campaign.active` (linkedin migration 0008) — `active=False` (ex. VIP, dont la recherche native globale ramassait ~38 profils lyonnais hors-cible/jour = 75% du budget lectures gâché) sort la campagne de `AccountSession.campaigns` (filtre `active=True`) : plus de seed connect, plus de qualifier, et le daemon draine ses tasks résiduelles sans les exécuter.
- **Anti-detection browser layer (EKOALU, refonte 11/06)**: real **Chrome** (`channel="chrome"`) via **Patchright** (NOT stock Playwright — fixes the CDP `Runtime.enable` leak + `navigator.webdriver`) in **`launch_persistent_context`** on `data/linkedin_profile/` (persistent cookies/localStorage/IndexedDB/fingerprint = stable trusted device). No `playwright-stealth` (counter-productive). First login is **manual** (`manage.py linkedin_manual_login` → Richard ticks "stay logged in" → `li_rm` device-trust cookie, 1yr); the daemon then reuses the profile and **never auto-logs-in with a password** (`reauthenticate()`/`_maybe_refresh_cookies()` are no-ops). Root cause of the per-login checkpoint: cookies injected into a fresh context = stolen-session signature. `is_authenticated()` dismisses two recurring overlays on the feed: `dismiss_comply_gate` ("Agree to comply" interstitial) and `dismiss_cookie_consent` (cookie privacy banner — clicks "Accept"; it reappeared every session because nothing clicked it, so the consent cookie was never persisted in the profile — 16/06). Full operating mode in `docs/MODE_OPERATOIRE_LINKEDIN.md`; source benchmark in `docs/ANNUAIRE_SOURCES_ANTI_DETECTION.md`.
- **Anti-ban guards (EKOALU)**: layered kill-switches checked by the daemon loop, all in `ekoalu/`. (1) `emergency_stop.py` — manual STOP sentinel `data/emergency_stop.flag`, never auto-purges. (2) `read_guard/` — **hard cap on LinkedIn profile reads/day** (`EKOALU_DAILY_PROFILE_READS_CAP`, default 40; prod **60** since 12/06, auto-ramps to **75 on 2026-06-15** via `EKOALU_PROFILE_READS_CAP_RAMP="YYYY-MM-DD:cap,..."`): patches `PlaywrightLinkedinAPI.get_profile`/`get_connection_degree` (count + raise `ReadCapExceededError` at cap), `ProfileReadDay` DB counter, daemon pauses tasks but keeps draining approved sends; reads were what triggered the 06/06 checkpoint, not sends. (2e) **modulation hebdo + jitter (LOT E 07/07)** — le cap lectures EFFECTIF du jour = nominal (env/ramp) × poids hebdo (`conf.WEEKDAY_WEIGHTS` : Lu/Ma/Je 1.0, Me 0.9, Ve 0.7, Sa 0.2, Di 0) × jitter journalier déterministe par date (0.85-1.0) — `ekoalu/human_scheduler/budget.py`, appliqué dans `read_guard/guard.py:daily_reads_cap()`. Avant : saturation au même cap TOUS les jours, samedi à 100 % compris = signature régulière (poids hebdo utilisés seulement comme booléen jour actif). Les caps d'envois quotidiens (2b) appliquent le poids hebdo SEUL (pas de jitter). Plancher `PACING_MIN_FLOOR` conservé. (2f) **jours off aléatoires (LOT E)** — 1-2 jours ouvrés/mois SANS AUCUNE action LinkedIn (lectures, envois, visites) : tirage déterministe par mois (seed = hash 'YYYY-MM' + sel env `EKOALU_HUMANIZE_SALT`), jamais 2 consécutifs, `budget.py:is_day_off()` consommé par `is_action_allowed_now()` et `next_active_window_start()` ; le canal email et les commandes manuelles restent actifs (les commandes email passent `channel="email"`). Kill-switch `EKOALU_RANDOM_DAYS_OFF=0`. Les pics gaussiens 10h/16h historiques (code mort, jamais branchés) ont été SUPPRIMÉS — le pacing linéaire (2c) + les jitters de délais suffisent (décision CTO KISS). (2c) **intra-day read pacing** (anti-burst matinal, remarque Richard 15/06) — `read_guard/guard.py:reads_budget_now()` débloque le cap journalier LINÉAIREMENT sur la plage active (07h-20h) au lieu de tout au réveil ; quand `is_paced_cap_reached()` est vrai, le daemon **laisse quand même passer les tasks `follow_up`** (LOT C 07/07 : elles lisent la DB — snapshot/résumé déjà matérialisés — pas LinkedIn ; toute lecture imprévue est stoppée par le cap DUR) et ne temporise ~10min (jitter) que s'il n'y a aucune follow_up prête → les lectures s'étalent en filet au lieu de partir en rafale à l'ouverture, sans affamer les relances. Plafond MOU (gating daemon, jamais de raise), distinct du cap dur. Kill-switch `EKOALU_READ_PACING=0` ; plancher `PACING_MIN_FLOOR=5`. (2d) **pacing intra-task** (LOT C) — toute paire de lectures Voyager consécutives d'une même task (enrichissement search `discover_and_enrich`, embeds URL-only) est espacée d'un tirage `enrich_min/max_delay_seconds` = **20-45s** (avant 6-10s = rafale) ; les délais ENTRE tasks restent au human_scheduler. (2b) **daily send caps** in `ekoalu/outbound_validation/sender.py:process_approved_queue` — invitations (`EKOALU_DAILY_INVITE_CAP`, 12/d prod) AND LinkedIn messages follow-up+reply (`EKOALU_DAILY_MESSAGE_CAP`, default 15/d, 2026 free-account safe zone 15-20/d); blocked kinds wait for the next day, other kinds keep flowing; both daily caps are modulated by the weekday weight (LOT E, cf. 2e — ex. samedi ×0.2). (3) `auth_watch.py` — **auto-STOP after N consecutive auth failures** (`EKOALU_AUTH_FAILURES_BEFORE_STOP`, default **1** = stop on first: insisting hardens the checkpoint): engages emergency_stop + mails Richard + leaves the browser window open for manual login. Counter resets on task success. (4) `llm_usage/budget_guard.py` — daily Claude budget ($4). (5) `llm_usage/api_limit_guard.py` — Anthropic org cap.
- **CRM integrity (EKOALU)**: `ekoalu/crm_integrity.py` + `manage.py check_crm_integrity [--fix]` — detects/repairs disqualified-lead-with-active-Deal, open PendingOutbound on dead leads/done deals, duplicate open POs; multi-active-deals reported (fix via `consolidate_duplicate_deals`). `manage.py triage_failed_outbound [--dry-run]` — one-shot triage of FAILED POs (reject obsolete, re-pend transients). Anomaly count + profile-reads counter surfaced in `daily_recap`. **Disqualification cascade (LOT D 07/07)**: `ekoalu/lead_exclusion.py:disqualify_leads` is the ONLY way to disqualify a lead (never set `Lead.disqualified=True` directly) — closes non-terminal Deals (params `outcome`/`deal_reason` for non-"refus Richard" paths), rejects open PendingOutbound, and cancels open check_pending/follow_up tasks (marked COMPLETED). All paths route through it: UI reject single/bulk, Django Admin, lead-detail "disqualify" button, `already_connected` action (outcome `pre_existing_relation`), connect Unreachable (`linkedin/db/leads.py:disqualify_lead`, outcome `unresponsive`), degree=1 sourcing auto-exclusion. Handlers are also no-op tolerant: `handle_follow_up` AND `handle_check_pending` close the deal and return without any LinkedIn read if the lead is disqualified. `Task.mark_failed()` stamps `completed_at` (the scheduler retry cap counts FAILED via `completed_at__gte` — it never fired before). A task whose campaign was deleted is marked FAILED at claim time by the daemon (no crash loop).
- **Email canal extras (EKOALU)**: `ekoalu/email_canal/bounce.py` — NDR/bounce detection in the inbox poller (postmaster/mailer-daemon senders or NDR subjects), marks `Lead.email_bounced_at`, excluded from sends by `sender._resolve_recipient`. Brique K: `PendingReply.cold_variant` copies the `prompt_variant` of the last sent cold mail for that lead (A/B reply-rate table in `daily_recap`). **Cold-mail idempotence (16/06)**: `generate_cold_emails` skips a lead if it is `disqualified` (a Richard reject = permanent exclusion via `lead_exclusion`) OR already has a cold mail in any of PENDING/APPROVED/SENDING/SENT/BLOCKED_COMPANY/**REJECTED** — without these two guards a rejected prospect was regenerated every day and resurfaced in the validation queue. **Shared exclusion list (LOT D 07/07)**: `ekoalu/shared_exclusions.py` reads `_partage/exclusions.json` (bounces + unsubscribes fed by mailing-mailjet; path via env `EKOALU_SHARED_EXCLUSIONS_PATH`, 60s TTL cache, missing/corrupt file = warning + empty list, lowercase compare). Consulted by `sender._resolve_recipient` (send blocked), `generate_cold_emails` (skip) and `import_mailjet_hot_leads` (skip). `_resolve_recipient` also re-checks `disqualified` at send time (symmetry with the LinkedIn sender). **Anti double-send claim (LOT D)**: `send_approved_emails`/`send_approved_email_replies` claim each row atomically APPROVED→**SENDING** (+`claimed_at`, ekoalu migration 0024) via conditional `UPDATE` (rowcount 0 = another overlapping Task Scheduler run took it → skip); a SENDING older than 2h at the next run = uncertain outcome → back to APPROVED with an EXPLICIT warning (possible duplicate, never silent). `import_mailjet_hot_leads` dedups emails case-insensitively on both sides.
- **Message learning loop (EKOALU, LOT B 07/07)**: `ekoalu/learning.py` — single selection path for CorrectionExample few-shot, wired into ALL generators: LinkedIn DM (`ekoalu/follow_up/generator.py`), cold email (`ekoalu/email_generator/generator.py` — corrections now actually feed the cold-mail prompt), email reply (`reply_generator.py`). Selection filters by **channel FIRST** (`CorrectionExample.channel`: linkedin_dm / email_cold / email_reply — migration ekoalu 0023 derives it from legacy slugs and blanks the pseudo-slugs follow_up/invitation/email_cold), then prefers `persona_slug` when it has >= 3 examples (else falls back to the whole channel — never another channel). Near-duplicate instructions (difflib ratio > 0.9 or long common prefix) are deduped so a consigne repeated 12x no longer saturates the 8-distinct-examples window; consignes recurring >= 3x are promoted to a `=== REGLES APPRISES ===` block **prepended to the system prompt**. Every injected example is marked `used_in_prompt=True` (dashboard counter). A **reject with an exploitable reason** (non-empty and not technical: "déjà en relation", "doublon", "déjà contacté"…) creates a `Kind.REJECTION` CorrectionExample (`views._capture_rejection_learning`) rendered as a negative example.
- **DM relance mode (EKOALU, LOT B 07/07)**: `generate_ekoalu_dm(relance=True)` — used when the Deal already has >= 1 outgoing ChatMessage (`follow_up/patch.py`) or the outbound is a REPLY (`views._regenerate_outbound_draft`). Lightened prompt (`follow_up/prompts.py:RELANCE_SYSTEM_PROMPT`): NO 4-bloc structure, NO EKOALU pitch/offer repetition (Richard's most-repeated consigne), react to conversation context, ONE useful info or question, sign "Richard" only (the full signature block is NOT re-appended in post-processing). The FIRST message keeps the rigid 4-bloc structure + niche mention.
- **Style guard (EKOALU, LOT B 07/07)**: `ekoalu/message_validator/style_guard.py:enforce_style` — post-generation validation on all 3 channels (banned words + "Cordialement"): on violation, ONE regeneration with the explicit motive; if it still violates, log warning and the message goes to Richard's validation queue anyway (never a hard silent block).
- **Qualifier feedback injection (EKOALU, LOT B 07/07)**: `ekoalu/qualification_feedback/injection.py:qualification_feedback_block` — the last 5 REQUALIFY/CONFIRM_REJECT QualificationFeedback (same campaign first, global fallback; ALREADY_CONNECTED excluded) are injected into the **USER** prompt of `qualify_with_llm(campaign_id=…)` (the system rubric stays byte-identical for Anthropic prompt caching), marked `used_in_prompt=True`.
- **Apify enrichment (EKOALU, wired 07/07, actor switched 15/07)**: `ekoalu/apify_enrich/` — cookieless enrichment of public LinkedIn profiles via the Apify actor `apimaestro~linkedin-profile-batch-scraper-no-cookies-required` ($5/1000, works on the FREE Apify plan, validated live 15/07; input `{usernames, includeEmail:false}`, output `basic_info`+`experience`, mapper dispatches on shape). Previous actor `harvestapi~linkedin-profile-scraper` ($4/1000) died 09/07: 20-runs-total limit for FREE Apify accounts. **Free-tier daily limit**: apimaestro caps FREE accounts at 10 profiles/DAY (resets tomorrow) — `client.ApifyDailyLimitError` + `service._saturate_today()` circuit-breaker fills the day counter so the daemon stops paying ~$0.005/attempt for error items and falls back to Voyager until midnight. **Not-found handling**: an explicit per-URL "No profile found" item passes through to the service which DISQUALIFIES the lead (outcome unresponsive) — otherwise it stays at the head of the FIFO backlog and is re-billed daily. Note: the actor dedups recently-scraped profiles per account (a retry returns nothing for them). **Qualification reads go through Apify; Richard's LinkedIn account is used for ENGAGEMENT ONLY** (pre-invite visits, invitations, messages, connection degrees, check_pending probes — unchanged). `client.py` calls the Apify API v2 `run-sync-get-dataset-items` endpoint in batches of 5 (env `EKOALU_APIFY_TOKEN` + `EKOALU_APIFY_ACTOR`); **ABSOLUTE RULE: never send our LinkedIn cookie/session to Apify — public URLs only** (locked by test). `mapper.py` maps the actor JSON to the internal `profile_snapshot` shape (`source: "apify"`; `service.py` adds `fetched_at`). `service.py:enrich_urlonly_leads/enrich_lead` selects URL-only leads (no snapshot, no embedding, not disqualified, real `linkedin.com/in/` URL, discovered by an ACTIVE campaign), stores snapshot + embedding via `Lead.embed_from_profile`. **Daily spend cap**: `ApifyUsageDay` counter (ekoalu 0025 + `failed` field 0026), env `EKOALU_APIFY_DAILY_CAP` (default 40/d); kill-switch `EKOALU_APIFY_ENRICH=0`; Apify fetches NEVER increment the read_guard cap (not an account read). **Failure refund (15/07)**: attempts are counted before the network call but failures are refunded via `service.record_failures()` (Greatest floor 0) and accumulated in `ApifyUsageDay.failed` — a broken actor (e.g. HarvestAPI's 20-run limit for FREE Apify accounts, which killed enrichment 10-15/07) no longer eats the cap for nothing, and `daily_recap` raises a top-priority "Apify HS" preco when failed >= 5 with 0 successes. NOTE: the HarvestAPI actor requires a PAID Apify plan beyond 20 total runs — decision pending (paid plan / different actor / Voyager-first). Wiring: `_embed_urlonly_leads` (qualify.py) is Apify-first with Voyager fallback on any Apify failure (lead left intact) and unchanged behavior when Apify is not configured; batch backlog via `manage.py apify_enrich_backlog [--max N] [--dry-run]` (scheduled through parent-repo `scripts/apify_enrich.ps1`, log `data/apify_enrich.log`). See `docs/APIFY_ENRICH.md`.
- **Fabricant detection (EKOALU, 28/07)**: `ekoalu/fabricant_detect/` + `manage.py detect_fabricants` — decides whether a company MANUFACTURES joinery in its own workshop or merely RESELLS and INSTALLS, by reading its website. Needed because the DECP `fabricant` flag is purely NAF-based (a 4-code list: 25.11Z, 25.12Z, 16.23Z, 22.23Z) and NAF is self-declared and never audited — false in both directions (3 industrials were flagged `cible_prioritaire` on 28/07: VERRERIES DU CENTRE, SIGNAUX GIROD, LADAME METALLERIE). **The rubric (`prompts.py`) is the deliverable** — it encodes the domain knowledge: (1) a broad catalogue (alu AND PVC AND wood) is a RESELLING marker, not a manufacturing one — nobody runs an alu line, a PVC welder and a wood shop at once (Richard 28/07); (2) buying profiles from a gammiste does NOT disqualify (EKOALU itself buys Cortizo/Sepalumic and manufactures) — the real discriminant is transforming in-house (cut/machine/assemble/glaze) vs installing finished products; (3) GAMMISTES (Cortizo, Sepalumic, SAPA, Wicona, Technal, Schüco = profile suppliers) are kept distinct from FINISHED-PRODUCT BRANDS (K-Line, Tryba, Janneau, Lapeyre… = reseller networks), the latter being a strong negoce signal; (4) "fabrication sur mesure" alone is NOT proof — every reseller says it; (5) the email domain may point at an unrelated site (observed: `SAS CLAUDE LAUMOND` → a Finnish writers' residence) → `indetermine`, never classify a company on someone else's website. **Cost model (Richard 28/07)**: Haiku 4.5 via the **Batch API** (−50%) for the bulk, escalating to Sonnet only on `indetermine` or low-confidence verdicts — ~$0.50 for 242 companies, well under the $4/day budget guard. `fetch.py` is dependency-free (httpx + regex HTML→text, no JS): homepage + up to 2 "société/savoir-faire/atelier" pages, following links actually present rather than hammering 404s; a site needing JS yields empty text → `indetermine`, which is the correct behavior. Verdicts persist in `FabricantVerdict` (ekoalu 0027) keyed by **SIREN**, not Lead — the verdict is about the COMPANY and survives re-import from another source. `is_fabricant` requires a non-low confidence. Fetch failures are persisted as `indetermine` so they are not re-scraped every pass.
- **Daily conformity check (15/07)**: `manage.py daily_conformity` — read-only morning test of the whole pipeline against explicit expectations (Apify enriched >= min(8, backlog) using ground truth `profile_snapshot__source="apify"`; Serper sourcing >= 15/working day; >= 1 connect served yesterday when due (qualification runs inside handle_connect); >= 1 deal created yesterday when candidates exist; no approved outbound stuck > 24h; overdue task backlog < 150). Every non-conformity carries a PROPOSED CORRECTION; mails Richard (graph_mailer) + writes `data/conformity_last.md` (re-read at session start). Scheduled task `EKOALU-Conformity-Check` 08:45 Mon-Fri via parent-repo `scripts/daily_conformity.ps1`, log `data/daily_conformity.log`.
- **Field encryption (EKOALU, Lot 2 RGPD 18/06)**: `ekoalu/crypto.py:EncryptedCharField` — transparent at-rest Fernet encryption (AES-128+HMAC). Key in env `EKOALU_FIELD_ENCRYPTION_KEY` (NEVER in DB; lives in `.env.production`). `SiteConfig.llm_api_key` uses it (ciphertext is `fernet:`-prefixed; legacy plaintext is read as-is and encrypted on next save — see migration `linkedin/0009_encrypt_secrets`). Degrades to plaintext (with a warning) if the key is absent, so the app never fails to boot. `LinkedInProfile.linkedin_password` is no longer stored (blanked) — manual login since 11/06 means no password is ever needed.
- **Config**: `SiteConfig` DB singleton (LLM_PROVIDER, LLM_API_KEY — chiffré at-rest, AI_MODEL, LLM_API_BASE — editable via Django Admin; `llm_provider` chooses between OpenAI/Anthropic/Google/Groq/Mistral/Cohere/openai_compatible, `llm_api_base` only consulted when provider is `openai_compatible`), `conf.py:CAMPAIGN_CONFIG` (timing/ML defaults), `conf.py` browser constants (`BROWSER_*`, `HUMAN_TYPE_*`), `conf.py` schedule constants (`ENABLE_ACTIVE_HOURS` flag, active hours/timezone/rest days), `conf.py` onboarding defaults (`DEFAULT_*_LIMIT`), `conf.py:FASTEMBED_CACHE_DIR` (persistent model cache, defaults to `<project>/.cache/fastembed/`), Campaign/LinkedInProfile models (Django Admin). `VOYAGER_REQUEST_TIMEOUT_MS` lives in `api/client.py` (constructor default on `PlaywrightLinkedinAPI`). `conf.py:DUMP_PAGES` (default `False`) — enable to save page HTML snapshots for fixture collection.
- **Lazy accessors**: `Lead.get_profile(session)` serves the stored `profile_snapshot` (JSON, crm 0015) when present and < 30 days old, else scrapes Voyager once and stores it (`refresh=True` forces a re-scrape). **1 lead = 1 LinkedIn read max** — LLM verdicts, follow-up summaries and message generation read from DB and keep working after the daily read cap (EKOALU 12/06; previously every call was a live scrape and a Serper lead cost 2 reads). `create_enriched_lead` stores the snapshot from the in-hand profile dict. `Lead.get_urn(session)` reads the `urn` column and falls back to a scrape; `Lead.get_embedding(session)` lazily scrapes + embeds on first access, then caches the 384-dim bytes on the row. `Lead.embed_from_profile(profile)` reuses an in-hand profile dict to skip the scrape (used by `create_enriched_lead`). `Lead.to_profile_dict()` returns a minimal `{lead_id, public_identifier, url, meta}` dict (no `profile` key). `AccountSession.campaigns` (cached_property, list). `AccountSession.self_profile` (cached_property, re-discovers via Voyager on first access per session — no DB cache).
- **Deal summaries**: `Deal.profile_summary` and `Deal.chat_summary` are lazy, mem0-style JSON fact lists built on demand and updated incrementally. `linkedin/db/summaries.py` is the single boundary — `materialize_profile_summary_if_missing(deal, session)` fires on the first follow-up touch (one Voyager re-scrape per `(lead, campaign)` lifetime), `update_chat_summary(deal, new_messages)` folds newly-synced ChatMessages into the summary via `reconcile_facts`, which routes new facts through mem0's UPDATE prompt to apply ADD/UPDATE/DELETE/NONE events (no naive append-and-dedup). **Only incoming (lead) messages reach fact extraction** — outgoing seller messages are filtered at the boundary so `chat_summary` stores facts about the lead, never the seller's pitch. A one-sided burst of outgoing messages short-circuits the LLM call entirely. The follow-up agent consumes `profile_summary + chat_summary + last 6 ChatMessage rows` instead of flat profile fields. The fact-extraction prompt is vendored at `linkedin/db/summaries.py:_FACT_EXTRACTION_PROMPT`; mem0's `DEFAULT_UPDATE_MEMORY_PROMPT` and `get_update_memory_messages` are vendored under `linkedin/vendor/mem0/configs/prompts.py` (mirroring upstream paths so future syncs are a clean diff). No `mem0ai` runtime dependency — avoids qdrant/grpcio/sqlalchemy transitive bloat.
- **Django apps**: `linkedin` (main — Campaign with users M2M), `crm` (Lead with embedding/Deal), `chat` (ChatMessage).
- **Data dir**: `data/` holds persistent state (`db.sqlite3`). Docker users mount volumes at `/app/data`.
- **Docker**: Playwright base image, VNC on port 5900, `BUILD_ENV` arg selects requirements.
- **CI/CD**: `.github/workflows/tests.yml` (pytest), `deploy.yml` (build + push to ghcr.io).
